# Seed images placeholder

Esta carpeta contendrá las imágenes cacheadas que se descargan durante `prisma/seed.ts`.
Las imágenes se sirven desde aquí para que el sitio cargue rápido sin depender
de servicios externos como picsum.photos.

Ver `src/lib/storage.ts` y `prisma/seed.ts` para más detalles.
